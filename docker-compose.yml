version: '3.3'

services:
  db:
    image: mysql:8
    restart: always
    environment:
      MYSQL_DATABASE: 'propertyprodb'
      # Password for root access
      MYSQL_ROOT_PASSWORD: 'pa55word'
    ports:
      # <Port exposed> : <MySQL Port running inside container>
      - '3306:3306'
    expose:
      # Opens port 3306 on the container
      - '3306'
    container_name: propertyprodb

    blog:
    image: momodayoo/propertyprodb:latest
    restart: unless-stopped
    environment:
      PORT: 3000
      DB_HOST: 'mysqldb'
      DB_USER: 'root'
      DB_PASS: 'pa55word'
      DB_NAME: 'propertyprodb'
      DB_SSL: 'false'
      BASE_URL: 'http://localhost:3000'
    depends_on:
      mysqldb:
        condition: service_started
    ports:
      - '3000:3000'
    container_name: propertyprodb